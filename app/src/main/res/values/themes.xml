<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MySubscribers" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#673AB7</item>
        <item name="colorPrimaryVariant">#532D97</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#3F51B5</item>
        <item name="colorSecondaryVariant">#37448E</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- Change ToolBar style. -->
        <item name="toolbarStyle">@style/ToolbarStyle</item>
    </style>

    <!-- Set toolbar style. -->
    <style name="ToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/ToolbarStyle.Title</item>
    </style>

    <!-- Set toolbar title style. -->
    <style name="ToolbarStyle.Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Set button style. -->
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_shape</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- Set button delete style. -->
    <style name="ButtonStyle.Delete" parent="ButtonStyle">
        <item name="android:background">@drawable/button_shape_delete</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>